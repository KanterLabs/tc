interface:
  display_name: "TC Roadmap (Helm compatibility)"
  short_description: "Compatibility alias for tracking work in Helm"
  default_prompt: "Use $tc-roadmap only for compatibility; prefer $helm to track this work."

policy:
  allow_implicit_invocation: true
